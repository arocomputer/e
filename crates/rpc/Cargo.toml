[package]
# The headless frontend: `ulo rpc`, a JSONL session server over stdin and stdout,
# and the turn result that `ulo -p --json` reports too.
name = "ulo-rpc"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
description = "The JSONL session server behind `ulo rpc`"
documentation = "https://ulo.sh/docs"
readme = "../../README.md"
keywords = ["agent", "coding-agent", "jsonl", "rpc"]
categories = ["development-tools"]

[lib]
name = "ulo_rpc"

[dependencies]
ulo-core.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
uuid.workspace = true
