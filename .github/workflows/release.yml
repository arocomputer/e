name: Release

# Stable tags and beta candidates qualify here; dev follows successful main CI.
on:
  push:
    tags: ["v*"]
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      action:
        description: Publish a beta candidate or retry existing release packages
        type: choice
        options: [beta, retry]
        default: beta
      commit:
        description: Beta source commit already on main (defaults to main)
        type: string
      tag:
        description: Existing version tag for a package retry
        type: string

permissions: {}

concurrency:
  group: release-publishing
  queue: max
  cancel-in-progress: false

jobs:
  resolve:
    name: Select release
    if: github.repository == 'intuitums/e' && (github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push'))
    runs-on: ubuntu-latest
    permissions:
      # GitHub requires push access to inspect unpublished draft releases.
      contents: write
    outputs:
      tag: ${{ steps.release.outputs.tag }}
      version: ${{ steps.release.outputs.version }}
      channel: ${{ steps.release.outputs.channel }}
      command: ${{ steps.release.outputs.command }}
      sha: ${{ steps.release.outputs.sha }}
      mode: ${{ steps.release.outputs.mode }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - id: release
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 scripts/release/resolve.py
  qualify:
    needs: resolve
    if: needs.resolve.outputs.mode == 'build'
    name: Qualify tag
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.resolve.outputs.sha }}

      - name: Verify
        env:
          CHANNEL: ${{ needs.resolve.outputs.channel }}
          TAG: ${{ needs.resolve.outputs.tag }}
        run: |
          ./x check
          ./x ui
          E_BUILD_CHANNEL="$CHANNEL" ./x release-check "$TAG"

  create:
    name: Draft release
    needs: [resolve, qualify]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.resolve.outputs.sha }}

      - name: Draft
        env:
          CHANNEL: ${{ needs.resolve.outputs.channel }}
          SHA: ${{ needs.resolve.outputs.sha }}
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.resolve.outputs.tag }}
        run: |
          if [ "$CHANNEL" = stable ]; then
            ./scripts/release-notes.sh "$TAG" < CHANGELOG.md > notes.md
          else
            printf 'Preview build %s from %s. See CHANGELOG.md at this commit for changes.\n' "$TAG" "$SHA" > notes.md
          fi
          if draft=$(gh release view "$TAG" --json isDraft --jq .isDraft 2>/dev/null); then
            test "$draft" = true || { echo 'Release is already published; use action=retry to retry packages without rebuilding.'; exit 1; }
          else
            gh release create "$TAG" --draft --target "$SHA" --title "$TAG" --notes-file notes.md
          fi

  build:
    name: Build (${{ matrix.runner }})
    needs: [resolve, create]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-latest
            targets: "aarch64-apple-darwin x86_64-apple-darwin"
          - runner: ubuntu-latest
            targets: "x86_64-unknown-linux-gnu"
          - runner: ubuntu-24.04-arm
            targets: "aarch64-unknown-linux-gnu"
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.resolve.outputs.sha }}

      - name: Build
        env:
          E_BUILD_VERSION: ${{ needs.resolve.outputs.version }}
          E_BUILD_CHANNEL: ${{ needs.resolve.outputs.channel }}
          E_BUILD_COMMIT: ${{ needs.resolve.outputs.sha }}
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.resolve.outputs.tag }}
        run: |
          for target in ${{ matrix.targets }}; do
            rustup target add "$target"
            cargo build --release --locked --target "$target"
            tar czf "e-$target.tar.gz" -C "target/$target/release" e
            gh release upload "$TAG" "e-$target.tar.gz" --clobber
          done

          host=$(rustc -vV | sed -n 's/^host: //p')
          case " ${{ matrix.targets }} " in
            *" $host "*)
              "target/$host/release/e" --version | grep -Fx "e ${TAG#v}"
              sha256sum "e-$host.tar.gz" > checksums.txt 2>/dev/null || \
                shasum -a 256 "e-$host.tar.gz" > checksums.txt
              install_dir=$(mktemp -d)
              E_RELEASE_BASE="file://$PWD" E_INSTALL_DIR="$install_dir" ./install.sh --channel "$E_BUILD_CHANNEL" --version "$E_BUILD_VERSION"
              "$install_dir/${{ needs.resolve.outputs.command }}" --version | grep -Fx "e ${TAG#v}"
              ;;
          esac

  publish:
    name: Checksums and publish
    needs: [resolve, build]
    if: ${{ !cancelled() && (needs.build.result == 'success' || needs.resolve.outputs.mode == 'recover') }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      attestations: write
      contents: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.resolve.outputs.sha }}

      - name: Download and checksum every artifact
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.resolve.outputs.tag }}
        run: |
          mkdir -p "$RUNNER_TEMP/release-assets"
          gh release download "$TAG" --pattern "e-*.tar.gz" --dir "$RUNNER_TEMP/release-assets"
          cd "$RUNNER_TEMP/release-assets"
          test "$(find . -name 'e-*.tar.gz' | wc -l)" -eq 4
          sha256sum e-*.tar.gz > checksums.txt

      - name: Generate CycloneDX SBOM
        run: |
          cargo install cargo-cyclonedx --version 0.5.9 --locked
          cargo cyclonedx --format json --spec-version 1.5
          # cargo-cyclonedx names the BOM after the crate (e.cdx.json), not bom.json.
          mv e.cdx.json "$RUNNER_TEMP/release-assets/e-sbom.cdx.json"

      - name: Attest release artifacts
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: ${{ runner.temp }}/release-assets/*

      - name: Upload metadata and publish
        env:
          CHANNEL: ${{ needs.resolve.outputs.channel }}
          SHA: ${{ needs.resolve.outputs.sha }}
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.resolve.outputs.tag }}
        run: |
          gh release upload "$TAG" "$RUNNER_TEMP/release-assets/checksums.txt" "$RUNNER_TEMP/release-assets/e-sbom.cdx.json" --clobber
          if [ "$CHANNEL" = stable ]; then
            ./scripts/release-notes.sh "$TAG" < CHANGELOG.md > notes.md
            python3 scripts/release/notes.py notes.md "$TAG" "$SHA" > "$RUNNER_TEMP/release-assets/release.json"
            gh release upload "$TAG" "$RUNNER_TEMP/release-assets/release.json" --clobber
            gh release edit "$TAG" --draft=false --latest
          else
            gh release edit "$TAG" --draft=false --prerelease --latest=false
          fi

  npm:
    name: npm and bun
    needs: [resolve, publish]
    if: ${{ !cancelled() && (needs.publish.result == 'success' || needs.resolve.outputs.mode == 'retry') }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.resolve.outputs.sha }}
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '24'
          registry-url: https://registry.npmjs.org
      - name: Prepare verified packages
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.resolve.outputs.tag }}
        run: |
          test "$(gh release view "$TAG" --json isDraft --jq '.isDraft')" = false
          gh release download "$TAG" --pattern 'e-*.tar.gz' --pattern checksums.txt --dir assets
          python3 scripts/packaging/prepare.py "$TAG" assets dist
      - name: Publish and verify npm packages
        env:
          # Remove this secret after all five packages trust this workflow.
          NODE_AUTH_TOKEN: ${{ secrets.NPM_BOOTSTRAP_TOKEN }}
        run: node scripts/packaging/publish-npm.mjs dist
      - name: Keep preview packages off latest
        if: needs.resolve.outputs.channel != 'stable'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_BOOTSTRAP_TOKEN }}
          VERSION: ${{ needs.resolve.outputs.version }}
        run: |
          # npm may assign latest on a package's first publication despite --tag.
          for package in dist/*/package.json; do
            name=$(node -p 'require("./" + process.argv[1]).name' "$package")
            latest=$(npm dist-tag ls "$name" --json | node -p 'JSON.parse(require("fs").readFileSync(0,"utf8")).latest || ""')
            if [ "$latest" = "$VERSION" ]; then
              npm dist-tag rm "$name" latest
            fi
          done

  homebrew:
    name: Homebrew tap
    permissions:
      contents: read
    needs: [resolve, publish]
    if: ${{ !cancelled() && (needs.publish.result == 'success' || needs.resolve.outputs.mode == 'retry') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.resolve.outputs.sha }}
      - name: Prepare verified formula
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.resolve.outputs.tag }}
        run: |
          test "$(gh release view "$TAG" --json isDraft --jq '.isDraft')" = false
          gh release download "$TAG" --pattern 'e-*.tar.gz' --pattern checksums.txt --dir assets
          python3 scripts/packaging/prepare.py "$TAG" assets dist
      - name: Check publishing access
        env:
          TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: test -n "$TAP_TOKEN" || { echo 'Configure HOMEBREW_TAP_TOKEN before publishing.'; exit 1; }
      - name: Check out tap
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: intuitums/homebrew-tap
          path: tap
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
      - name: Publish formula
        env:
          TAG: ${{ needs.resolve.outputs.tag }}
        run: scripts/packaging/publish-brew.sh "dist/${{ needs.resolve.outputs.command }}.rb" tap

  channel:
    name: Advance channel and verify website installer
    needs: [resolve, publish, npm, homebrew]
    if: ${{ !cancelled() && needs.npm.result == 'success' && needs.homebrew.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.resolve.outputs.sha }}
      - name: Advance channel
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.resolve.outputs.tag }}
        run: python3 scripts/release/channel.py "$TAG"
      - name: Install through website
        env:
          TAG: ${{ needs.resolve.outputs.tag }}
          CHANNEL: ${{ needs.resolve.outputs.channel }}
          COMMAND: ${{ needs.resolve.outputs.command }}
        run: |
          curl -fsSL --retry 3 https://e.intuitum.sh/install.sh -o "$RUNNER_TEMP/install.sh"
          E_INSTALL_DIR="$RUNNER_TEMP/e-pinned" sh "$RUNNER_TEMP/install.sh" --channel "$CHANNEL" --version "${TAG#v}"
          "$RUNNER_TEMP/e-pinned/$COMMAND" --version | grep -Fx "e ${TAG#v}"
          E_INSTALL_DIR="$RUNNER_TEMP/e-current" sh "$RUNNER_TEMP/install.sh" --channel "$CHANNEL"
          "$RUNNER_TEMP/e-current/$COMMAND" --version
